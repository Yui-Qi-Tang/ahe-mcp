// Package evidenceimplements validates versioned implementation-relation reviews.
// Pure review values and receipts are assertions, not persisted admissions.
// Native writers reload admitted endpoints and atomically persist exact reviewed
// edges and independent receipts. MCP exposes the bounded recursive-derived
// profile. Legacy contract identifiers are retained for byte compatibility.
package evidenceimplements
